---
export interface Props {
	image: {
		href: string | URL;
		alt?: string;
	};
}

const { image } = Astro.props;
---

<figure>
	<Img src={image.href} alt={image.alt ?? ''} preload="avif" format="webp" loading="lazy" />
	<figcaption>
		<slot />
	</figcaption>
</figure>
